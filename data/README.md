# Data

This directory is reserved for any local data artifacts (e.g. exported CSVs used during seeding review).
The actual 20,000+ record dataset lives in PostgreSQL and is generated by `app/database/seed.py` / `scripts/seed_database.py` — it is not committed here.
